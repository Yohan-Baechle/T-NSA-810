---
# Usage: ansible-playbook playbooks/filebeat.yml --ask-vault-pass
# Le rôle common est appliqué automatiquement via la dépendance meta.

- name: Deploy Filebeat log shipping
  hosts: all
  roles:
    - filebeat

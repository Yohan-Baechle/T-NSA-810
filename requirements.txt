# ansible-lint 24.x importe encore ansible.parsing.yaml.constructor, supprimé
# dans ansible-core 2.18+. On borne donc à <2.18 (même contrainte que la CI).
ansible-core>=2.16,<2.18
ansible-lint>=24,<25
yamllint>=1,<2
pynetbox>=7,<8
pre-commit>=3,<4
